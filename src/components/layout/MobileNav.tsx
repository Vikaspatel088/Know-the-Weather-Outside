import React from 'react'; import { CloudSun, CalendarDays, Radar, AlertTriangle } from 'lucide-react'; import './MobileNav.css';
interface Props { activeTab: 'today'|'forecast'|'radar'|'alerts'; onSelectTab: (tab: 'today'|'forecast'|'radar'|'alerts') => void; hasActiveAlerts?: boolean; }
export const MobileNav: React.FC<Props> = ({activeTab,onSelectTab,hasActiveAlerts}) => <nav className="atmos-mobile-nav">{[['today',CloudSun,'Today'],['forecast',CalendarDays,'Forecast'],['radar',Radar,'Radar'],['alerts',AlertTriangle,'Alerts']].map(([tab,Icon,label]) => { const Item = Icon as React.ElementType; return <button key={tab as string} className={activeTab === tab ? 'active' : ''} onClick={() => onSelectTab(tab as Props['activeTab'])}><Item size={20}/><span>{label as string}{tab === 'alerts' && hasActiveAlerts ? ' •' : ''}</span></button>; })}</nav>;
